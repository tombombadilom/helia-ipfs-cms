
const  {jest, expect, describe, it } = require('@jest/global');
describe('Réseau', () => {
  it('devrait effectuer une opération avec le réseau avec succès', async () => {
    // Ajoute des tests avec le réseau ici
    // Utilisez expect pour les assertions
  });
});
